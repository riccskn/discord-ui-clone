import styled from "styled-components";

export const Container = styled.div`

   grid-area: CL;
   height: 100%;
   background: #2F3136;
`

export const ChannelTitleBox = styled.div`

   height: 46px;
   background: #2F3136;
   box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.5);
   display: flex;

`

export const ChannelTitle = styled.h2`

  @import url('https://fonts.googleapis.com/css2?family=Roboto&display=swap');
  
  margin-left: 16px;
  margin-top: 13.5px;
  font-family: 'Roboto', sans-serif;
  font-weight: 700;
  font-size: 16px;
  white-space: nowrap;
  line-height: 19px;

  color: #FFFFF8;


`

export const TextChannelsList = styled.div`

  width: 100%;
  height: 35px;
  margin-top: 24px; 
  display: flex;
  
`

export const TextChannelsTitle = styled.p`

    font-size: 12px;  
  
    font-family: 'Roboto', sans-serif;
    font-weight: 500;
    padding-left: 16px;
    color: #828386;

`